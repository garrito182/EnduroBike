//SORT
/* let ordenarAS = document.getElementById("btnOrdenarAS");
ordenarAS.onclick = () => {
    let biciAs = bicis
    biciAs.sort((a, b) => {
        if (a.marca == b.marca) {
            return 0;
        }
        if (a.marca < b.marca) {
            return -1;
        }
        return 1;
    });
}

let ordenarDes = document.getElementById("btnOrdenarDes");
ordenarDes.onclick = () => {
    let biciDes = bicis
    biciDes.sort((a, b) => {
        if (a.marca == b.marca) {
            return 0;
        }
        if (a.marca > b.marca) {
            return -1;
        }
        return 1;
    });
}

let ordenarPrecioAs = document.getElementById("btnPrecioAs");
ordenarPrecioAs.onclick = () => {
    let precioAs = bicis
    precioAs.sort((a, b) => {
        if (a.precio == b.precio) {
            return 0;
        }
        if (a.precio < b.precio) {
            return -1;
        }
        return 1;
    });
}

let ordenarPrecioDes = document.getElementById("btnPrecioDes");
ordenarPrecioDes.onclick = () => {
    let precioAs = bicis
    let precioDes = bicis
    precioDes.sort((a, b) => {
        if (a.precio == b.precio) {
            return 0;
        }
        if (a.precio > b.precio) {
            return -1;
        }
        return 1;
    })
} */